= bootstrap_form_for podcast, url: admin_podcast_path(podcast.id) do |f|
  = f.text_field :title
  = f.text_field :artist
  = f.text_field :thumbnail_src
  = f.text_field :large_cover_src
  = f.text_area :embed_code
  = f.text_area :tracklist

  = f.submit

